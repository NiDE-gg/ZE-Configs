"entities"
{
	"0"
	{
		"name"			"Ammo Box" //name of the item to show in chat
		"shortname"			"Ammo" //name of the item to show in scoreboard
		"color"			"{#2F2F2F}" //colour to use in chat (refer to the colour list below)
		"buttonclass"			"func_button" //what classname the button entity uses, usually just func_button
		"filtername"			"player_ammo" //filtername that the items filter entity uses (if applicable, see below)
		"hasfiltername"			"false" //true if the item uses a filter entity to check the user, false otherwise
		"blockpickup"			"false" //always false
		"allowtransfer"			"true" //true for human items, false for zm items
		"forcedrop"			"true" //true for human items, false for zm items
		"chat"			"true" //always true
		"hud"			"true" //always true
		"hammerid"			"345207" //hammerid of the weapon_ entity for the item
		"mode"			"2" //0 = nothing 1 = spam protection only, 2 = cooldown, 3 = limited uses, 4 = limited uses with cooldown, 5 = cooldown that only gets triggered after all maxuses are used
		"maxuses"			"0" //max uses of the item (if applicable)
		"cooldown"			"70" //cooldown of the item (if applicable)
		"maxamount"			"1" //how many instances of this item can exist
		"physbox"			"" //OPTIONAL: "true" if this item is a physbox so it would allow bullets/knife to shoot/knife through. If it's false, dont bother adding this line.
	}

	"1"
	{
		"name"			"Human Slow" //name of the item to show in chat
		"shortname"			"Slow" //name of the item to show in scoreboard
		"color"			"{#B40404}" //colour to use in chat (refer to the colour list below)
		"buttonclass"			"func_button" //what classname the button entity uses, usually just func_button
		"filtername"			"player_human_canon_slow" //filtername that the items filter entity uses (if applicable, see below)
		"hasfiltername"			"false" //true if the item uses a filter entity to check the user, false otherwise
		"blockpickup"			"false" //always false
		"allowtransfer"			"true" //true for human items, false for zm items
		"forcedrop"			"true" //true for human items, false for zm items
		"chat"			"true" //always true
		"hud"			"true" //always true
		"hammerid"			"360408" //hammerid of the weapon_ entity for the item
		"mode"			"2" //0 = nothing 1 = spam protection only, 2 = cooldown, 3 = limited uses, 4 = limited uses with cooldown, 5 = cooldown that only gets triggered after all maxuses are used
		"maxuses"			"0" //max uses of the item (if applicable)
		"cooldown"			"25" //cooldown of the item (if applicable)
		"maxamount"			"1" //how many instances of this item can exist
		"physbox"			"" //OPTIONAL: "true" if this item is a physbox so it would allow bullets/knife to shoot/knife through. If it's false, dont bother adding this line.
	}

	"2"
	{
		"name"			"Human Push" //name of the item to show in chat
		"shortname"			"Push" //name of the item to show in scoreboard
		"color"			"{#FF0000}" //colour to use in chat (refer to the colour list below)
		"buttonclass"			"func_button" //what classname the button entity uses, usually just func_button
		"filtername"			"player_push" //filtername that the items filter entity uses (if applicable, see below)
		"hasfiltername"			"false" //true if the item uses a filter entity to check the user, false otherwise
		"blockpickup"			"false" //always false
		"allowtransfer"			"true" //true for human items, false for zm items
		"forcedrop"			"true" //true for human items, false for zm items
		"chat"			"true" //always true
		"hud"			"true" //always true
		"hammerid"			"343671" //hammerid of the weapon_ entity for the item
		"mode"			"2" //0 = nothing 1 = spam protection only, 2 = cooldown, 3 = limited uses, 4 = limited uses with cooldown, 5 = cooldown that only gets triggered after all maxuses are used
		"maxuses"			"0" //max uses of the item (if applicable)
		"cooldown"			"70" //cooldown of the item (if applicable)
		"maxamount"			"1" //how many instances of this item can exist
		"physbox"			"" //OPTIONAL: "true" if this item is a physbox so it would allow bullets/knife to shoot/knife through. If it's false, dont bother adding this line.
	}

	"3"
	{
		"name"			"Human Gravity" //name of the item to show in chat
		"shortname"			"Gravity" //name of the item to show in scoreboard
		"color"			"{#0000FF}" //colour to use in chat (refer to the colour list below)
		"buttonclass"			"func_button" //what classname the button entity uses, usually just func_button
		"filtername"			"player_gravity" //filtername that the items filter entity uses (if applicable, see below)
		"hasfiltername"			"false" //true if the item uses a filter entity to check the user, false otherwise
		"blockpickup"			"false" //always false
		"allowtransfer"			"true" //true for human items, false for zm items
		"forcedrop"			"true" //true for human items, false for zm items
		"chat"			"true" //always true
		"hud"			"true" //always true
		"hammerid"			"343797" //hammerid of the weapon_ entity for the item
		"mode"			"2" //0 = nothing 1 = spam protection only, 2 = cooldown, 3 = limited uses, 4 = limited uses with cooldown, 5 = cooldown that only gets triggered after all maxuses are used
		"maxuses"			"0" //max uses of the item (if applicable)
		"cooldown"			"70" //cooldown of the item (if applicable)
		"maxamount"			"1" //how many instances of this item can exist
		"physbox"			"" //OPTIONAL: "true" if this item is a physbox so it would allow bullets/knife to shoot/knife through. If it's false, dont bother adding this line.
	}

	"4"
	{
		"name"			"Human Laser Wall" //name of the item to show in chat
		"shortname"			"Laser Wall" //name of the item to show in scoreboard
		"color"			"{#00FF00}" //colour to use in chat (refer to the colour list below)
		"buttonclass"			"func_button" //what classname the button entity uses, usually just func_button
		"filtername"			"player_laser_wall" //filtername that the items filter entity uses (if applicable, see below)
		"hasfiltername"			"false" //true if the item uses a filter entity to check the user, false otherwise
		"blockpickup"			"false" //always false
		"allowtransfer"			"true" //true for human items, false for zm items
		"forcedrop"			"true" //true for human items, false for zm items
		"chat"			"true" //always true
		"hud"			"true" //always true
		"hammerid"			"344388" //hammerid of the weapon_ entity for the item
		"mode"			"2" //0 = nothing 1 = spam protection only, 2 = cooldown, 3 = limited uses, 4 = limited uses with cooldown, 5 = cooldown that only gets triggered after all maxuses are used
		"maxuses"			"0" //max uses of the item (if applicable)
		"cooldown"			"70" //cooldown of the item (if applicable)
		"maxamount"			"1" //how many instances of this item can exist
		"physbox"			"" //OPTIONAL: "true" if this item is a physbox so it would allow bullets/knife to shoot/knife through. If it's false, dont bother adding this line.
	}

	"5"
	{
		"name"			"Human Freezer" //name of the item to show in chat
		"shortname"			"Freezer" //name of the item to show in scoreboard
		"color"			"{#FF0000}" //colour to use in chat (refer to the colour list below)
		"buttonclass"			"func_button" //what classname the button entity uses, usually just func_button
		"filtername"			"player_freezer" //filtername that the items filter entity uses (if applicable, see below)
		"hasfiltername"			"false" //true if the item uses a filter entity to check the user, false otherwise
		"blockpickup"			"false" //always false
		"allowtransfer"			"true" //true for human items, false for zm items
		"forcedrop"			"true" //true for human items, false for zm items
		"chat"			"true" //always true
		"hud"			"true" //always true
		"hammerid"			"344508" //hammerid of the weapon_ entity for the item
		"mode"			"2" //0 = nothing 1 = spam protection only, 2 = cooldown, 3 = limited uses, 4 = limited uses with cooldown, 5 = cooldown that only gets triggered after all maxuses are used
		"maxuses"			"0" //max uses of the item (if applicable)
		"cooldown"			"70" //cooldown of the item (if applicable)
		"maxamount"			"1" //how many instances of this item can exist
		"physbox"			"" //OPTIONAL: "true" if this item is a physbox so it would allow bullets/knife to shoot/knife through. If it's false, dont bother adding this line.
	}


	"6"
	{
		"name"			"Zombie Slow" //name of the item to show in chat
		"shortname"			"Slow" //name of the item to show in scoreboard
		"color"			"{#2F2F2F}" //colour to use in chat (refer to the colour list below)
		"buttonclass"			"func_button" //what classname the button entity uses, usually just func_button
		"filtername"			"player_zslow" //filtername that the items filter entity uses (if applicable, see below)
		"hasfiltername"			"false" //true if the item uses a filter entity to check the user, false otherwise
		"blockpickup"			"false" //always false
		"allowtransfer"			"false" //true for human items, false for zm items
		"forcedrop"			"false" //true for human items, false for zm items
		"chat"			"true" //always true
		"hud"			"true" //always true
		"hammerid"			"360754" //hammerid of the weapon_ entity for the item
		"mode"			"2" //0 = nothing 1 = spam protection only, 2 = cooldown, 3 = limited uses, 4 = limited uses with cooldown, 5 = cooldown that only gets triggered after all maxuses are used
		"maxuses"			"0" //max uses of the item (if applicable)
		"cooldown"			"20" //cooldown of the item (if applicable)
		"maxamount"			"1" //how many instances of this item can exist
		"physbox"			"" //OPTIONAL: "true" if this item is a physbox so it would allow bullets/knife to shoot/knife through. If it's false, dont bother adding this line.
	}

	"7"
	{
		"name"			"Zombie Gravity" //name of the item to show in chat
		"shortname"			"Gravity" //name of the item to show in scoreboard
		"color"			"{#B40404}" //colour to use in chat (refer to the colour list below)
		"buttonclass"			"func_button" //what classname the button entity uses, usually just func_button
		"filtername"			"player_zgravity" //filtername that the items filter entity uses (if applicable, see below)
		"hasfiltername"			"false" //true if the item uses a filter entity to check the user, false otherwise
		"blockpickup"			"false" //always false
		"allowtransfer"			"false" //true for human items, false for zm items
		"forcedrop"			"false" //true for human items, false for zm items
		"chat"			"true" //always true
		"hud"			"true" //always true
		"hammerid"			"344108" //hammerid of the weapon_ entity for the item
		"mode"			"2" //0 = nothing 1 = spam protection only, 2 = cooldown, 3 = limited uses, 4 = limited uses with cooldown, 5 = cooldown that only gets triggered after all maxuses are used
		"maxuses"			"0" //max uses of the item (if applicable)
		"cooldown"			"90" //cooldown of the item (if applicable)
		"maxamount"			"1" //how many instances of this item can exist
		"physbox"			"" //OPTIONAL: "true" if this item is a physbox so it would allow bullets/knife to shoot/knife through. If it's false, dont bother adding this line.
	}

	"8"
	{
		"name"			"Zombie Heal" //name of the item to show in chat
		"shortname"			"Heal" //name of the item to show in scoreboard
		"color"			"{#FF0000}" //colour to use in chat (refer to the colour list below)
		"buttonclass"			"func_button" //what classname the button entity uses, usually just func_button
		"filtername"			"player_zheal" //filtername that the items filter entity uses (if applicable, see below)
		"hasfiltername"			"false" //true if the item uses a filter entity to check the user, false otherwise
		"blockpickup"			"false" //always false
		"allowtransfer"			"false" //true for human items, false for zm items
		"forcedrop"			"false" //true for human items, false for zm items
		"chat"			"true" //always true
		"hud"			"true" //always true
		"hammerid"			"344235" //hammerid of the weapon_ entity for the item
		"mode"			"2" //0 = nothing 1 = spam protection only, 2 = cooldown, 3 = limited uses, 4 = limited uses with cooldown, 5 = cooldown that only gets triggered after all maxuses are used
		"maxuses"			"0" //max uses of the item (if applicable)
		"cooldown"			"50" //cooldown of the item (if applicable)
		"maxamount"			"1" //how many instances of this item can exist
		"physbox"			"" //OPTIONAL: "true" if this item is a physbox so it would allow bullets/knife to shoot/knife through. If it's false, dont bother adding this line.
	}

	"9"
	{
		"name"			"Zombie Ring" //name of the item to show in chat
		"shortname"			"Ring" //name of the item to show in scoreboard
		"color"			"{#0000FF}" //colour to use in chat (refer to the colour list below)
		"buttonclass"			"func_button" //what classname the button entity uses, usually just func_button
		"filtername"			"player_zring" //filtername that the items filter entity uses (if applicable, see below)
		"hasfiltername"			"false" //true if the item uses a filter entity to check the user, false otherwise
		"blockpickup"			"false" //always false
		"allowtransfer"			"false" //true for human items, false for zm items
		"forcedrop"			"false" //true for human items, false for zm items
		"chat"			"true" //always true
		"hud"			"true" //always true
		"hammerid"			"344584" //hammerid of the weapon_ entity for the item
		"mode"			"2" //0 = nothing 1 = spam protection only, 2 = cooldown, 3 = limited uses, 4 = limited uses with cooldown, 5 = cooldown that only gets triggered after all maxuses are used
		"maxuses"			"0" //max uses of the item (if applicable)
		"cooldown"			"110" //cooldown of the item (if applicable)
		"maxamount"			"1" //how many instances of this item can exist
		"physbox"			"" //OPTIONAL: "true" if this item is a physbox so it would allow bullets/knife to shoot/knife through. If it's false, dont bother adding this line.
	}

	"10"
	{
		"name"			"Zombie Freezeball" //name of the item to show in chat
		"shortname"			"Freezeball" //name of the item to show in scoreboard
		"color"			"{#00FF00}" //colour to use in chat (refer to the colour list below)
		"buttonclass"			"func_button" //what classname the button entity uses, usually just func_button
		"filtername"			"player_zfreeze" //filtername that the items filter entity uses (if applicable, see below)
		"hasfiltername"			"false" //true if the item uses a filter entity to check the user, false otherwise
		"blockpickup"			"false" //always false
		"allowtransfer"			"false" //true for human items, false for zm items
		"forcedrop"			"false" //true for human items, false for zm items
		"chat"			"true" //always true
		"hud"			"true" //always true
		"hammerid"			"344796" //hammerid of the weapon_ entity for the item
		"mode"			"2" //0 = nothing 1 = spam protection only, 2 = cooldown, 3 = limited uses, 4 = limited uses with cooldown, 5 = cooldown that only gets triggered after all maxuses are used
		"maxuses"			"0" //max uses of the item (if applicable)
		"cooldown"			"50" //cooldown of the item (if applicable)
		"maxamount"			"1" //how many instances of this item can exist
		"physbox"			"" //OPTIONAL: "true" if this item is a physbox so it would allow bullets/knife to shoot/knife through. If it's false, dont bother adding this line.
	}

	"11"
	{
		"name"			"Fireball" //name of the item to show in chat
		"shortname"			"Fireball" //name of the item to show in scoreboard
		"color"			"{#FF0000}" //colour to use in chat (refer to the colour list below)
		"buttonclass"			"func_button" //what classname the button entity uses, usually just func_button
		"filtername"			"player_zfire" //filtername that the items filter entity uses (if applicable, see below)
		"hasfiltername"			"false" //true if the item uses a filter entity to check the user, false otherwise
		"blockpickup"			"false" //always false
		"allowtransfer"			"false" //true for human items, false for zm items
		"forcedrop"			"false" //true for human items, false for zm items
		"chat"			"true" //always true
		"hud"			"true" //always true
		"hammerid"			"345034" //hammerid of the weapon_ entity for the item
		"mode"			"2" //0 = nothing 1 = spam protection only, 2 = cooldown, 3 = limited uses, 4 = limited uses with cooldown, 5 = cooldown that only gets triggered after all maxuses are used
		"maxuses"			"0" //max uses of the item (if applicable)
		"cooldown"			"35" //cooldown of the item (if applicable)
		"maxamount"			"1" //how many instances of this item can exist
		"physbox"			"" //OPTIONAL: "true" if this item is a physbox so it would allow bullets/knife to shoot/knife through. If it's false, dont bother adding this line.
	}

}